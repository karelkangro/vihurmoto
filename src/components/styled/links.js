import styled from 'styled-components';

export const SocialLink = styled.a`
  img {
    width: 2rem;
    height: 2rem;

    &:hover {
      transform: scale(1.2);
      transition: all .25s ease-out;
    }
  }
`;